"""English — the source language. Keys are the English strings themselves, so
this catalog is intentionally empty: every lookup falls back to the key."""

NAME = "English"

STRINGS: dict[str, str] = {}
